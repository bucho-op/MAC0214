#include<bits/stdc++.h>
#define int long long
using namespace std;


signed main(){
    int n, k; cin >> n >> k; vector<int> before(n+1), after(n+1);
    // for (int i = 1; i<=n; i++){
    //     int v; cin >> v; before[i] = v; after[i] = v;
    // }
    // for (int i = 0; i<n; i++) cout << after[i] << " ";
    // cout << endl;
    for (int i = 0; i < k; i++){
        cout << "A" << endl;
    }

    // for (int i = 1; i<n; i++) cout << after[i] << " ";
    // cout << after[n] << endl;
}